---
name: typescript-react
description: "Typescript React for rssokhla. 15 conventions."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **25 real patterns** in **rssokhla**. This skill is auto-routed to agents when working on typescript-react files.

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Strengthened types NotificationFeed — parallelizes async operations for speed** — - import {
+ import NotificationFeed from "../../islands/NotificationFeed.tsx";
-   calculateMonthly
- 📐 **Strengthened types COMPLIANCE — prevents null/undefined runtime crashes** — -             {/* ── NOTIFICATIONS TAB ─────────────────────────────── */}
+             {/* ── COMP
- 📐 **Strengthened types EmbedWidgetModal — formalizes the data contract with expli...** — - 
+ import EmbedWidgetModal from "../../islands/EmbedWidgetModal.tsx";
- interface PropertiesData {
- 📐 **Strengthened types MutationObserver** — -       </Head>
+         <script dangerouslySetInnerHTML={{ __html: `
- 
+           function repor
- 📐 **Strengthened types Head — improves module reusability** — - import { type Handlers, type PageProps } from "$fresh/server.ts";
+ import { Head } from "$fresh/r
- 📐 **convention in [propId].tsx** — -           <meta property="og:image" content={property.imageUrl} />
+           <meta property="og:
- 📐 **Strengthened types TLDR — ensures atomic multi-step database operations** — -           {/* Content */}
+           {/* TLDR Banner */}
-           <div class="prose-legal">
+ 
- 📐 **Strengthened types OtaSavingsChart** — - import OtaSavingsChart from "../../islands/OtaSavingsChart.tsx";
+ import OtaSavingsChart, { type 
- 📐 **Strengthened types Team — prevents null/undefined runtime crashes** — -     ...(role === "owner" || role === "manager" ? [
+     ...(role === "owner" || role === "manager
- 📐 **Strengthened types Booking — parallelizes async operations for speed** — - import { listNotifications, getDashboardStats, listBookings, listProperties, getPropertyViewsDaily
- 📐 **Strengthened types EarningsComparison — parallelizes async operations for speed** — - 
+ import EarningsComparison from "../../islands/EarningsComparison.tsx";
- interface OverviewData
- 📐 **Strengthened types ComponentChildren — parallelizes async operations for speed** — - import {
+ import { type ComponentChildren } from "preact";
-   getDashboardStats,
+ import {
-   
- 📐 **Strengthened types PricingCheckout — prevents null/undefined runtime crashes** — - 
+ import PricingCheckout from "../islands/PricingCheckout.tsx";
- const SCHEMA = JSON.stringify({
- 📐 **Strengthened types Person — optimizes data fetching with relational query ins...** — -     "@type": "Organization",
+     "@type": "Person",
-     name: "Chishti Ventures Pvt. Ltd.",
+ 
- 📐 **Strengthened types Person — ensures atomic multi-step database operations** — -     "@type": "Organization",
+     "@type": "Person",
-     name: "Chishti Ventures Pvt. Ltd.",
+ 

## 🔵 Architecture

### how-it-works in index.tsx
-         <div class="bg-white rounded-[2.5rem] p-1 border-2 border-gray-100 shadow-sm overflow-hidden">
+         <div class="bg-white rounded-[2.5rem] p-1 border-[3px] border-gray-900 shadow-[10px_1

### how-it-works in index.tsx
-           <div class="bg-white rounded-[2rem] p-7 border-2 border-gray-100 shadow-sm">
+           <div class="bg-white rounded-[2.5rem] p-8 border-[3px] border-gray-900 shadow-[8px_8px_0px_0px_rgba

### how-it-works in NotificationFeed.tsx
-     <div class="lg:col-span-2 bg-white rounded-[2.5rem] border-2 border-gray-100 shadow-sm overflow-hidden flex flex-col">
+     <div class="lg:col-span-2 bg-white rounded-[2.5rem] border-[3px] bord

### how-it-works in DashboardSidebar.tsx
-             <SidebarContent />
+             {/* Logo Mobile */}
-           </aside>
+             <div class="flex items-center px-6 h-20 border-b border-gray-100 flex-shrink-0">
-         </>
+  

---
*Auto-generated by BrainSync 🧠 | 25 patterns | 2026-05-01*
